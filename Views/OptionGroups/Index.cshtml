@model IEnumerable<Ecommerce.Models.OptionGroup>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.OptionGroupName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.OptionGroupName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.OptionGroupId }) |
            @Html.ActionLink("Details", "Details", new { id=item.OptionGroupId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.OptionGroupId })
        </td>
    </tr>
}

</table>
