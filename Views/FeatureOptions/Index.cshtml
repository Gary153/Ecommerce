@model IEnumerable<Ecommerce.Models.FeatureOption>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.FeatureOptionsName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.FeatureOptionsName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.FeatureOptionId }) |
            @Html.ActionLink("Details", "Details", new { id=item.FeatureOptionId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.FeatureOptionId })
        </td>
    </tr>
}

</table>
