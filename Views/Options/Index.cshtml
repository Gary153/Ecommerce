@model IEnumerable<Ecommerce.Models.Option>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.OptionName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.OptionName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.OptionsId }) |
            @Html.ActionLink("Details", "Details", new { id=item.OptionsId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.OptionsId })
        </td>
    </tr>
}

</table>
